Build with `podman build --tag quay.io/mmarusak/experimment:2 .` (change tag)

Push with `podman push quay.io/mmarusak/experimment:2` (change tag)

Deploy with `oc create -f schema.yaml` (change tag)

Connect with `node client.js` (change url)
